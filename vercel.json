{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "VITE_DEMO_MODE=true npm run build -- --mode vercel",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
